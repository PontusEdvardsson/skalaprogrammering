# Uppgift 30

Listan ar:

```python
vikt = [1, 4, 5, 10]
```

Ratt ihopparning:

- `print(vikt[0])` -> `1`
- `print(vikt[3])` -> `10`
- `print(len(vikt))` -> `4`
- `print(sum(vikt) / len(vikt))` -> `5`

